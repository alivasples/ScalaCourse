import scala.annotation.tailrec

object S2 {

  /** Q1: Using Tail Recursion, convert a positive integer number
   * into a binary string number.
   * */


  /** Q2: Using Tail Recursion, convert an unsigned binary string
   * into a decimal integer number
   * */
  

  /** Q3: Using Tail Recursion, reverse a string */
  

  /** Q4: Using tail recursion, create a function that
   * reads a base (double), an exponent (positive and integer),
   * and compute its power (you must only use the
   * exponentiation definition, not math pow)
   * */
  

  /** Q5: Using Tail Recursion. Define a function to verify if a text contains a key as a substring.
   * DO NOT USE the .contains method of String object */
  

  /** Q6: Using Tail Recursion. Reverse a String each n characters */
  

  /** Q7: Using Tail Recursion. Verify if a string is a Palindrome or not */
  

  /** Q8: Define a expression that verifies if two numbers are relatively prime */
  

  /** Q9: Given an expression and using tail recursion, validate if all parenthesis are correct.
   * You only need to verify if parenthesis are in correct order and all of them
   * are closed at the end. No need to verify the expression itself. 
   * */
  

  /** Q10: Define a expression for adding two binary numbers (strings) */
  

  /** Main object */
  def main(args: Array[String]) = {
    
  }
}